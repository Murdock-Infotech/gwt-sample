server.port=8080
server.servlet.context-path=/

# Client-facing config (used by the host page + GWT client to target the RPC servlet).
# Default requested: localhost:8080 as the "context root" / origin for server calls.
modularwebapp.contextRoot=http://localhost:8080


